export const activitySeries = (groups = [], range = 7) => groups.slice(0, range).reverse().map((item) => ({ key: item.key, total: Number(item.total || 0) }));
export const debtRanking = (barrios = [], metric = "total") => [...barrios].map((item) => ({ name: item.barrio || item.nombre || "Sin barrio", value: metric === "accounts" ? Number(item.deuda?.deudores || 0) : metric === "critical" ? Number(item.deuda?.criticos || 0) : Number(item.deuda?.total || 0) })).filter((item) => item.value > 0).sort((a, b) => b.value - a.value).slice(0, 5);
